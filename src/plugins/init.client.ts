export default defineNuxtPlugin({
  name: 'init',
  setup(app) {
    const { getCatalog } = useCatalog()

    // const { getReviews } = useReviews()

    //  getCatalog()

    // getReviews()
  },
})
